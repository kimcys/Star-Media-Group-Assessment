# Copy this to .env.production ON THE SERVER ONLY and fill in real values.
# .env.production is gitignored - it must never be committed.
#
# Generate real random passwords, e.g.: openssl rand -base64 24

DB_NAME=star_assessment
DB_USER=star_app
DB_PASSWORD=change-me-generate-a-real-random-password
MYSQL_ROOT_PASSWORD=change-me-generate-a-different-real-random-password

APP_ENV=production
APP_DEBUG=false
CORS_ALLOWED_ORIGIN=https://aimanhakimcy.com
COOKIE_DOMAIN=aimanhakimcy.com
